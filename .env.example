# Google Maps JavaScript API key, restricted by HTTP referrer.
# NEXT_PUBLIC_ because the map runs in the browser - this value is public by design.
# Set the same variable in the Vercel project settings (all environments).
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
